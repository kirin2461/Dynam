modules = ["bash"]
[agent]
expertMode = true

[nix]
channel = "stable-25_05"
packages = ["cmake", "pkg-config", "libsodium", "libpcap", "sqlite"]

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Build and Run NCP CLI"

[[workflows.workflow]]
name = "Build and Run NCP CLI"
author = "agent"

[workflows.workflow.metadata]
outputType = "console"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "cd build && cmake .. -DENABLE_CLI=ON -DENABLE_GUI=OFF && make -j$(nproc) && ./bin/ncp help"
